package main

import "fmt"

func main() {

	numeros := [3]int{5, 6, 7}

	soma := 0

	for _, numeros := range numeros {

		soma += numeros

	}
	fmt.Println("os números armazenados na array são:", numeros)
	fmt.Println(" A soma dos inteiros amarzenados na array é de :", soma)

}
